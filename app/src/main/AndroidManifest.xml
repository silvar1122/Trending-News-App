<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newspaperapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Fruits"></activity>
        <activity android:name=".Coronavirus"></activity>
        <activity android:name=".Infinix"></activity>
        <activity android:name=".Series"></activity>
        <activity android:name=".TheProfile"></activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".Homepage"></activity>
    </application>

</manifest>